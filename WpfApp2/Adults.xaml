<Page x:Class="WpfApp2.Adults"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Adults">

    <Grid Background="White">
        <DataGrid x:Name="dg1" AutoGenerateColumns="False" Margin="10 0 10 10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер" Width="100" Binding="{Binding Path=id}">
                    
                </DataGridTextColumn>
                <DataGridTextColumn Header="Название услуги" Width="500" Binding="{Binding Path=Name}" >
                    
                </DataGridTextColumn>

                <DataGridTextColumn Header="Стоимость услуги" Width="200" Binding="{Binding Path=Cout}" >
    
                    
                </DataGridTextColumn>
                
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
